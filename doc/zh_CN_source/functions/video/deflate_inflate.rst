放气/充气
===============

.. function:: Deflate(vnode clip[, int[] planes=[0, 1, 2], float threshold])
   :module: std

   将每个像素替换为其 3x3 邻域中八个像素的平均值，但仅当该平均值小于中心像素时。

   *夹子*
      要处理的片段。必须具有 8 到 16 位之间的整数采样类型，或 32 位的浮点采样类型。如果存在其他格式的帧，将返回错误。
      
   *飞机*
      指定要处理的平面。未处理的平面将被简单复制。

   *临界点*
      允许限制像素变化量。输出像素不会小于 ``input - threshold``。默认无限制。


.. function:: Inflate(vnode clip[, int[] planes=[0, 1, 2], int threshold=65535])
   :module: std

   将每个像素替换为其 3x3 邻域中八个像素的平均值，但仅当该平均值大于中心像素时。

   *夹子*
      剪辑以进行处理。它必须具有整数样本类型和位深度
      8 到 16 之间，或浮点样本类型和位深度 32。如果
      如果有其他格式的帧，将会出现错误
      回来了。

   *飞机*
      指定将处理哪些平面。未处理的平面将被简单复制。

   *临界点*
      允许限制像素变化量。输出像素不会大于 ``input + threshold``。默认无限制。
