堆栈垂直/堆栈水平
=============================

.. function:: StackVertical(vnode[] clips)
              StackHorizontal(vnode[] clips)
   :module: std

   将所有给定的 *clips* 部分在一起。要求相同的格式。StackVertical 还要求所有片段宽度相同，StackHorizo​​ntal 要求所有片段高度相同。

   帧属性从第一个片段复制。
