音频修剪
=========

.. function::   AudioTrim(anode clip[, int first=0, int last, int length])
   :module: std
   
   AudioTrim 对音频片段可执行两个操作，但单位是采样乐器帧。

   在Python中，std.AudioTrim也可以通过 :ref:`切片 <pythonreference>` 调用。
